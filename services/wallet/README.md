# Wallet (API Hub Pro) — por importar
El código vive hoy en la PC del dueño (`Desktop\Hermes\`: `api_pro.py`, `hermes_llm_router.py`). Importarlo **sin tokens ni llaves**; usar `.env` (ignorado) y `.env.example`.
Pendientes antes de vender: cambiar la llave maestra de fábrica y construir el validador de proveedores. Ver `docs/DEUDA_TECNICA.md`.
